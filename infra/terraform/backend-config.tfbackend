# Terraform Backend Configuration File
# Use this with: terraform init -backend-config=backend-config.tfbackend
# This allows dynamic backend configuration in CI/CD

bucket         = "wordpress-swarm-terraform-state"
key            = "infrastructure/terraform.tfstate"
region         = "us-east-1"
encrypt        = true
dynamodb_table = "wordpress-swarm-terraform-locks"
