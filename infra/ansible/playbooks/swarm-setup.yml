---
# Swarm cluster initialization only (assumes Docker already installed)

- name: Initialize Docker Swarm cluster
  hosts: swarm
  become: true
  roles:
    - swarm-init

- name: Create Docker secrets
  hosts: swarm_managers
  become: true
  roles:
    - swarm-secrets
