---
# Ansible Galaxy requirements
# Install with: ansible-galaxy install -r requirements.yml

# Collections
collections:
  - name: community.general
    version: ">=7.0.0"

  - name: community.docker
    version: ">=3.4.0"

  - name: ansible.posix
    version: ">=1.5.0"

# Roles (if using external roles from Galaxy)
roles: []
  # Example:
  # - name: geerlingguy.docker
  #   version: "6.1.0"
